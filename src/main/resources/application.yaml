spring:
  application:
    name: testProducts

  datasource:
    url: jdbc:postgresql://localhost:5431/postgres
    username: "postgres"
    password: "postgres"
  thymeleaf:
    cache: false
  flyway:
    enabled: true
    locations: classpath:db/migration